"use client";

import Image from "next/image";
import { useMemo, useState } from "react";
import {
  ABILITIES, ALIGNMENTS, ARMOR, GENERAL_GEAR, NON_WEAPON_PROFICIENCIES, RACES, WEAPONS,
  type AbilityKey, type AbilityScores, type ArmorId, type ClassId, type RaceId, type WeaponId,
  applyRaceAdjustments, charismaDerived, classById, constitutionDerived, constitutionHpBonus,
  dexterityAcAdjustment, dexterityDerived, intelligenceDerived, levelOneSavingThrows,
  qualifiesForClass, raceById, startingGoldDice, strengthDerived, wisdomDerived,
} from "./rules";

type RollMethod = "classic" | "heroic" | "manual";
type StageId = "name" | "abilities" | "ancestry" | "class" | "alignment" | "features" | "training" | "vitals" | "equipment" | "record";
type ManualScores = Record<AbilityKey, string>;
type AbilityRolls = Partial<Record<AbilityKey, number[]>>;
type TrayRoll = { title: string; dice: number[]; sides: number; dropped?: number; formula: string; result: string };
type ThiefSkill = "pickPockets" | "openLocks" | "findTraps" | "moveSilently" | "hideShadows" | "detectNoise" | "climbWalls" | "readLanguages";

const SHIELD_COST = 10;
const EMPTY_SCORES: AbilityScores = { str: 0, dex: 0, con: 0, int: 0, wis: 0, cha: 0 };
const EMPTY_MANUAL: ManualScores = { str: "", dex: "", con: "", int: "", wis: "", cha: "" };
const EMPTY_THIEF: Record<ThiefSkill, number> = { pickPockets:0, openLocks:0, findTraps:0, moveSilently:0, hideShadows:0, detectNoise:0, climbWalls:0, readLanguages:0 };

const STAGES: { id: StageId; number: number; label: string; hint: string; icon: string }[] = [
  { id:"name", number:1, label:"Name", hint:"Define who you are", icon:"/assets/forge/icons/icon-record.png" },
  { id:"abilities", number:2, label:"Abilities", hint:"Roll your potential", icon:"/assets/forge/icons/icon-dice.png" },
  { id:"ancestry", number:3, label:"Ancestry", hint:"Choose your heritage", icon:"/assets/forge/icons/icon-ancestry.png" },
  { id:"class", number:4, label:"Class Path", hint:"Choose your calling", icon:"/assets/forge/icons/icon-class.png" },
  { id:"alignment", number:5, label:"Alignment", hint:"Shape your nature", icon:"/assets/forge/icons/icon-alignment.png" },
  { id:"features", number:6, label:"Class Training", hint:"Shape class talents", icon:"/assets/forge/icons/icon-star.png" },
  { id:"training", number:7, label:"Proficiencies", hint:"Learn your skills", icon:"/assets/forge/icons/icon-training.png" },
  { id:"vitals", number:8, label:"Vitals & Purse", hint:"Life and resources", icon:"/assets/forge/icons/icon-vitals.png" },
  { id:"equipment", number:9, label:"Equipment", hint:"Gear for adventure", icon:"/assets/forge/icons/icon-equipment.png" },
  { id:"record", number:10, label:"Final Record", hint:"Review your hero", icon:"/assets/forge/icons/icon-record.png" },
];

const RACE_ART: Record<RaceId,string> = {
  human:"/assets/forge/races/race-human.png", dwarf:"/assets/forge/races/race-dwarf.png", elf:"/assets/forge/races/race-elf.png",
  gnome:"/assets/forge/races/race-gnome.png", "half-elf":"/assets/forge/races/race-half-elf.png", halfling:"/assets/forge/races/race-halfling.png",
};
const CLASS_ART: Record<ClassId,string> = {
  fighter:"/assets/forge/classes/class-fighter.png", ranger:"/assets/forge/classes/class-ranger.png", paladin:"/assets/forge/classes/class-paladin.png",
  mage:"/assets/forge/classes/class-mage.png", illusionist:"/assets/forge/classes/class-illusionist.png", cleric:"/assets/forge/classes/class-cleric.png",
  druid:"/assets/forge/classes/class-druid.png", thief:"/assets/forge/classes/class-thief.png", bard:"/assets/forge/classes/class-bard.png",
};
const STAT_ART = { hp:"/assets/forge/icons/badge-hp.png", ac:"/assets/forge/icons/badge-ac.png", thac0:"/assets/forge/icons/badge-thac0.png", gold:"/assets/forge/icons/badge-gold.png" };

const THIEF_SKILLS: { id:ThiefSkill; label:string; base:number }[] = [
  {id:"pickPockets",label:"Pick Pockets",base:15},{id:"openLocks",label:"Open Locks",base:10},{id:"findTraps",label:"Find / Remove Traps",base:5},{id:"moveSilently",label:"Move Silently",base:10},
  {id:"hideShadows",label:"Hide in Shadows",base:5},{id:"detectNoise",label:"Detect Noise",base:15},{id:"climbWalls",label:"Climb Walls",base:60},{id:"readLanguages",label:"Read Languages",base:0},
];
const THIEF_RACE: Partial<Record<RaceId,Partial<Record<ThiefSkill,number>>>> = {
  dwarf:{openLocks:10,findTraps:15,climbWalls:-10,readLanguages:-5}, elf:{pickPockets:5,openLocks:-5,moveSilently:5,hideShadows:10,detectNoise:5},
  gnome:{openLocks:5,findTraps:10,moveSilently:5,hideShadows:5,detectNoise:10,climbWalls:-15}, "half-elf":{pickPockets:10,hideShadows:5},
  halfling:{pickPockets:5,openLocks:5,findTraps:5,moveSilently:10,hideShadows:15,detectNoise:5,climbWalls:-15,readLanguages:-5},
};
const WIZARD_SPELLS = ["Armor","Burning Hands","Charm Person","Color Spray","Comprehend Languages","Feather Fall","Grease","Identify","Light","Magic Missile","Phantasmal Force","Shield","Sleep","Spider Climb","Unseen Servant","Ventriloquism"];
const ILLUSION_SPECIALTY = ["Color Spray","Phantasmal Force","Ventriloquism"];
const PRIEST_SPELLS = ["Bless","Command","Cure Light Wounds","Detect Evil","Detect Magic","Light","Protection from Evil","Purify Food & Drink","Sanctuary"];
const DRUID_SPELLS = ["Animal Friendship","Detect Magic","Entangle","Faerie Fire","Pass without Trace","Shillelagh"];

function rollDie(sides:number){ return Math.floor(Math.random()*sides)+1; }
function rollDice(count:number,sides:number){ return Array.from({length:count},()=>rollDie(sides)); }
function signed(value:number){ return value>0?`+${value}`:`${value}`; }
function classPathLabel(ids:ClassId[]){ return ids.length?ids.map((id)=>classById(id).name).join(" / "):"Unchosen"; }
function stageIndex(id:StageId){ return STAGES.findIndex((stage)=>stage.id===id); }
function dexThiefAdjustment(dex:number,id:ThiefSkill){
  if (!["pickPockets","openLocks","findTraps","moveSilently","hideShadows"].includes(id)) return 0;
  const rows:Record<number,Partial<Record<ThiefSkill,number>>> = {
    9:{pickPockets:-15,openLocks:-10,findTraps:-10,moveSilently:-20,hideShadows:-10},10:{pickPockets:-10,openLocks:-5,findTraps:-10,moveSilently:-15,hideShadows:-5},
    11:{pickPockets:-5,findTraps:-5,moveSilently:-10},12:{moveSilently:-5},16:{openLocks:5},17:{pickPockets:5,openLocks:10,moveSilently:5,hideShadows:5},18:{pickPockets:10,openLocks:15,findTraps:5,moveSilently:10,hideShadows:10},19:{pickPockets:15,openLocks:20,findTraps:10,moveSilently:15,hideShadows:15},
  };
  if (dex>=13&&dex<=15) return 0; return rows[Math.max(9,Math.min(19,dex))]?.[id]??0;
}
function dieAsset(sides:number){ return `/assets/forge/dice/die-d${[4,6,8,10,12,20].includes(sides)?sides:20}.svg`; }

function DiceFace({value,sides,dropped,rolling}:{value:number;sides:number;dropped?:boolean;rolling?:boolean}){
  return <div className={`physical-die d${sides} ${dropped?"dropped":""} ${rolling?"rolling":""}`} style={{backgroundImage:`url('${dieAsset(sides)}')`}} aria-label={`d${sides} rolled ${value}`}><span>{value}</span><small>d{sides}</small></div>;
}
function DiceTray({roll,rolling,onRoll,actionLabel}:{roll:TrayRoll|null;rolling:boolean;onRoll?:()=>void;actionLabel?:string}){
  return <div className="dice-tray" aria-live="polite"><div className="dice-tray-top"><div><span className="tray-kicker">THE DICE TRAY</span><h3>{roll?.title??"Ready to Roll"}</h3></div><Image src="/assets/forge/icons/icon-dice.png" alt="" width={54} height={54}/></div><div className="dice-felt">{roll?roll.dice.map((value,index)=><DiceFace key={`${roll.title}-${index}`} value={value} sides={roll.sides} dropped={roll.dropped===index} rolling={rolling}/>):<p className="tray-empty">Your dice will land here.</p>}</div>{roll?<div className="dice-result"><span>{roll.formula}</span><strong>{roll.result}</strong></div>:null}{onRoll?<button type="button" className="rpg-button primary dice-throw-button" disabled={rolling} onClick={onRoll}>{rolling?"Rolling…":actionLabel??"Roll Dice"}</button>:null}<p className="dice-help">Nothing rolls until you throw the dice.</p></div>;
}

export default function CharacterForge(){
  const [stage,setStage]=useState<StageId>("name");
  const [name,setName]=useState(""); const [rollMethod,setRollMethod]=useState<RollMethod|null>(null); const [baseScores,setBaseScores]=useState<AbilityScores>(EMPTY_SCORES); const [manualScores,setManualScores]=useState<ManualScores>(EMPTY_MANUAL); const [abilityRolls,setAbilityRolls]=useState<AbilityRolls>({}); const [scoresGenerated,setScoresGenerated]=useState(false);
  const [raceId,setRaceId]=useState<RaceId|null>(null); const [classIds,setClassIds]=useState<ClassId[]>([]); const [alignment,setAlignment]=useState("");
  const [fighterDecision,setFighterDecision]=useState<"none"|"specialize"|null>(null); const [fighterSpecialization,setFighterSpecialization]=useState<WeaponId|null>(null); const [thiefPoints,setThiefPoints]=useState<Record<ThiefSkill,number>>(EMPTY_THIEF); const [wizardBookSize,setWizardBookSize]=useState<number|null>(null); const [wizardSpells,setWizardSpells]=useState<string[]>([]); const [illusionSpell,setIllusionSpell]=useState<string|null>(null); const [priestSpells,setPriestSpells]=useState<string[]>([]); const [featuresConfirmed,setFeaturesConfirmed]=useState(false);
  const [exceptionalStrength,setExceptionalStrength]=useState<number|null>(null); const [hp,setHp]=useState<number|null>(null); const [startingGold,setStartingGold]=useState<number|null>(null); const [weaponProfs,setWeaponProfs]=useState<WeaponId[]>([]); const [nonWeaponProfs,setNonWeaponProfs]=useState<string[]>([]); const [armorId,setArmorId]=useState<ArmorId|null>(null); const [shield,setShield]=useState(false); const [gear,setGear]=useState<string[]>([]); const [eventLog,setEventLog]=useState<string[]>([]); const [sealed,setSealed]=useState(false); const [trayRoll,setTrayRoll]=useState<TrayRoll|null>(null); const [rolling,setRolling]=useState(false);

  const race=raceId?raceById(raceId):null; const finalScores=useMemo(()=>scoresGenerated&&raceId?applyRaceAdjustments(baseScores,raceId):baseScores,[baseScores,raceId,scoresGenerated]); const classes=classIds.map(classById); const primaryClass=classes[0]??null; const isMulticlass=classIds.length>1; const classesQualified=scoresGenerated&&classIds.length>0&&classIds.every((id)=>qualifiesForClass(finalScores,id)); const alignmentAllowed=alignment!==""&&classes.length>0&&classes.every((item)=>!item.allowedAlignments||item.allowedAlignments.includes(alignment)); const raceAllowsPath=Boolean(race&&classIds.length>0&&(classIds.length===1?race.availableClasses.includes(classIds[0]):race.multiclass.some((path)=>path.length===classIds.length&&path.every((id,i)=>id===classIds[i]))));
  const canExceptional=scoresGenerated&&classes.some((item)=>item.exceptionalStrength)&&finalScores.str===18; const weaponSlots=classes.length?Math.min(...classes.map((item)=>item.weaponSlots)):0; const nonWeaponSlots=classes.length?Math.max(...classes.map((item)=>item.nonWeaponSlots)):0; const allowedWeapons=classes.length?WEAPONS.filter((weapon)=>classIds.every((id)=>classById(id).allowedWeapons.includes(weapon.id))):[]; const allowedArmor=classes.length?ARMOR.filter((armor)=>classIds.every((id)=>classById(id).allowedArmor.includes(armor.id))):[]; const shieldAllowed=classes.length>0&&classes.every((item)=>item.shield); const armor=armorId?ARMOR.find((item)=>item.id===armorId)??null:null; const dexAc=scoresGenerated?dexterityAcAdjustment(finalScores.dex):0; const ac=scoresGenerated&&classes.length?Math.max(-10,(armor?.ac??10)+dexAc-(shield?1:0)):null; const thac0=classes.length?20:null;
  const specializationExtra=fighterDecision==="specialize"&&fighterSpecialization?(fighterSpecialization==="shortbow"||fighterSpecialization==="longbow"?2:1):0; const usedWeaponSlots=weaponProfs.length+specializationExtra; const spent=useMemo(()=>weaponProfs.reduce((sum,id)=>sum+(WEAPONS.find((w)=>w.id===id)?.cost??0),0)+gear.reduce((sum,id)=>sum+(GENERAL_GEAR.find((g)=>g.id===id)?.cost??0),0)+(armor?.cost??0)+(shield?SHIELD_COST:0),[weaponProfs,gear,armor,shield]); const goldRemaining=startingGold===null?null:startingGold-spent;
  const thiefAllocated=Object.values(thiefPoints).reduce((a,b)=>a+b,0); const hasThief=classIds.includes("thief"); const hasWizard=classIds.includes("mage")||classIds.includes("illusionist"); const hasPriest=classIds.includes("cleric")||classIds.includes("druid"); const singleFighter=classIds.length===1&&classIds[0]==="fighter"; const priestSlots=hasPriest?1+(finalScores.wis>=14?2:finalScores.wis>=13?1:0):0; const wizardTarget=wizardBookSize??0; const wizardKnownCount=wizardSpells.length+2;
  const fighterFeatureComplete=!singleFighter||fighterDecision==="none"||(fighterDecision==="specialize"&&Boolean(fighterSpecialization)); const thiefFeatureComplete=!hasThief||thiefAllocated===60; const wizardFeatureComplete=!hasWizard||(wizardBookSize!==null&&wizardKnownCount===wizardTarget&&(!classIds.includes("illusionist")||Boolean(illusionSpell))); const priestFeatureComplete=!hasPriest||priestSpells.length===priestSlots; const needsOnlyConfirm=!singleFighter&&!hasThief&&!hasWizard&&!hasPriest; const classFeaturesComplete=fighterFeatureComplete&&thiefFeatureComplete&&wizardFeatureComplete&&priestFeatureComplete&&(!needsOnlyConfirm||featuresConfirmed);
  const trainingComplete=classes.length>0&&usedWeaponSlots===weaponSlots&&nonWeaponProfs.length===nonWeaponSlots; const vitalsComplete=hp!==null&&startingGold!==null&&(!canExceptional||exceptionalStrength!==null); const equipmentComplete=startingGold!==null&&armorId!==null&&goldRemaining!==null&&goldRemaining>=0; const nextAbilityIndex=ABILITIES.findIndex((ability)=>!abilityRolls[ability.id]); const currentAbility=nextAbilityIndex>=0?ABILITIES[nextAbilityIndex]:null;
  const hasWarriorClass=classes.some((item)=>item.group==="Warrior"); const strEffects=scoresGenerated?strengthDerived(finalScores.str,exceptionalStrength):null; const dexEffects=scoresGenerated?dexterityDerived(finalScores.dex):null; const conEffects=scoresGenerated?constitutionDerived(finalScores.con,hasWarriorClass):null; const intEffects=scoresGenerated?intelligenceDerived(finalScores.int):null; const wisEffects=scoresGenerated?wisdomDerived(finalScores.wis):null; const chaEffects=scoresGenerated?charismaDerived(finalScores.cha):null; const savingThrows=scoresGenerated?levelOneSavingThrows(classIds,raceId,finalScores.con):null;

  function log(text:string){ setEventLog((current)=>[text,...current].slice(0,5)); }
  function animateRoll(after:()=>void){ if(rolling)return; setRolling(true); window.setTimeout(()=>{after();setRolling(false);},620); }
  function resetAfterClass(){ setAlignment("");setFighterDecision(null);setFighterSpecialization(null);setThiefPoints(EMPTY_THIEF);setWizardBookSize(null);setWizardSpells([]);setIllusionSpell(null);setPriestSpells([]);setFeaturesConfirmed(false);setExceptionalStrength(null);setHp(null);setStartingGold(null);setWeaponProfs([]);setNonWeaponProfs([]);setArmorId(null);setShield(false);setGear([]);setSealed(false); }
  function clearAfterAbilityChange(){ setClassIds([]); resetAfterClass(); }
  function chooseRollMethod(method:RollMethod){ setRollMethod(method);setScoresGenerated(false);setBaseScores(EMPTY_SCORES);setManualScores(EMPTY_MANUAL);setAbilityRolls({});setTrayRoll(null);clearAfterAbilityChange();log(`${method==="classic"?"3d6 in order":method==="heroic"?"4d6 drop lowest":"Manual assignment"} selected.`); }
  function rollNextAbility(){ if(!rollMethod||rollMethod==="manual"||!currentAbility)return; const dice=rollDice(rollMethod==="classic"?3:4,6); const dropped=rollMethod==="heroic"?dice.indexOf(Math.min(...dice)):undefined; const total=dice.reduce((sum,value,index)=>sum+(index===dropped?0:value),0); setTrayRoll({title:`Roll for ${currentAbility.name}`,dice,sides:6,dropped,formula:rollMethod==="heroic"?`${dice.join(" + ")} · lowest discarded`:dice.join(" + "),result:`${currentAbility.short} ${total}`}); animateRoll(()=>{const key=currentAbility.id;const updated={...abilityRolls,[key]:dice};setAbilityRolls(updated);setBaseScores((scores)=>({...scores,[key]:total}));setScoresGenerated(ABILITIES.every((ability)=>Boolean(updated[ability.id])));clearAfterAbilityChange();log(`${currentAbility.name} rolled: ${total}.`);}); }
  function changeManualScore(key:AbilityKey,value:string){ const digits=value.replace(/\D/g,"").slice(0,2); const next={...manualScores,[key]:digits};setManualScores(next); const complete=ABILITIES.every((ability)=>{const score=Number.parseInt(next[ability.id],10);return Number.isFinite(score)&&score>=3&&score<=18;}); if(complete){const converted=ABILITIES.reduce((scores,ability)=>{scores[ability.id]=Number.parseInt(next[ability.id],10);return scores;},{...EMPTY_SCORES});setBaseScores(converted);setScoresGenerated(true);clearAfterAbilityChange();}else setScoresGenerated(false); }
  function chooseRace(id:RaceId){setRaceId(id);setClassIds([]);resetAfterClass();log(`${raceById(id).name} ancestry chosen.`);} function choosePath(ids:ClassId[]){setClassIds(ids);resetAfterClass();log(`${classPathLabel(ids)} chosen.`);}
  function chooseSpecialization(id:WeaponId|null){ if(id===null){setFighterDecision("none");setFighterSpecialization(null);return;} setFighterDecision("specialize");setFighterSpecialization(id);setWeaponProfs([id]);log(`${WEAPONS.find((w)=>w.id===id)?.name} specialization reserved.`); }
  function adjustThief(id:ThiefSkill,delta:number){ setThiefPoints((current)=>{const next=Math.max(0,Math.min(30,current[id]+delta));const total=Object.values(current).reduce((a,b)=>a+b,0)-current[id]+next;if(total>60)return current;return {...current,[id]:next};}); }
  function rollWizardBook(){const dice=rollDice(3,4);const total=dice.reduce((a,b)=>a+b,0);setTrayRoll({title:"Starting Spellbook Size",dice,sides:4,formula:dice.join(" + "),result:`${total} spells`});animateRoll(()=>{setWizardBookSize(total);setWizardSpells([]);setIllusionSpell(null);log(`Spellbook size rolled: ${total}.`);});}
  function toggleWizardSpell(spell:string){ if(!wizardBookSize)return; setWizardSpells((current)=>current.includes(spell)?current.filter((s)=>s!==spell):current.length<Math.max(0,wizardBookSize-2)?[...current,spell]:current); }
  function togglePriestSpell(spell:string){setPriestSpells((current)=>current.includes(spell)?current.filter((s)=>s!==spell):current.length<priestSlots?[...current,spell]:current);}
  function rollExceptionalStrength(){const tens=rollDie(10)-1,units=rollDie(10)-1;const result=tens===0&&units===0?100:tens*10+units;setTrayRoll({title:"Exceptional Strength",dice:[tens,units],sides:10,formula:`${tens}0 + ${units}`,result:`18/${String(result).padStart(2,"0")}`});animateRoll(()=>setExceptionalStrength(result));}
  function rollHp(){if(!classes.length)return;const raw=classes.map((c)=>rollDie(c.hitDie));const adjusted=raw.map((value,index)=>Math.max(1,value+constitutionHpBonus(finalScores.con,classes[index].group==="Warrior")));const result=Math.max(1,Math.floor(adjusted.reduce((a,b)=>a+b,0)/adjusted.length));const same=classes.every((c)=>c.hitDie===classes[0].hitDie);setTrayRoll({title:"Starting Hit Points",dice:raw,sides:same?classes[0].hitDie:20,formula:isMulticlass?`Class dice ${raw.join(" + ")} · CON · averaged`:`d${classes[0].hitDie} ${raw[0]} ${signed(constitutionHpBonus(finalScores.con,classes[0].group==="Warrior"))} CON`,result:`${result} HP`});animateRoll(()=>setHp(result));}
  function rollGold(){if(!primaryClass)return;const spec=startingGoldDice(primaryClass.group);const dice=rollDice(spec.count,spec.sides);const result=(dice.reduce((a,b)=>a+b,0)+spec.bonus)*spec.multiplier;setTrayRoll({title:"Starting Gold",dice,sides:spec.sides,formula:`(${dice.join(" + ")}${spec.bonus?` + ${spec.bonus}`:""}) × ${spec.multiplier}`,result:`${result} gp`});animateRoll(()=>{setStartingGold(result);setArmorId(null);setShield(false);setGear([]);});}
  function toggleWeapon(id:WeaponId){setWeaponProfs((current)=>{if(id===fighterSpecialization&&current.includes(id))return current;if(current.includes(id))return current.filter((x)=>x!==id);const projected=current.length+1+specializationExtra;return projected<=weaponSlots?[...current,id]:current;});} function toggleNwp(value:string){setNonWeaponProfs((current)=>current.includes(value)?current.filter((x)=>x!==value):current.length<nonWeaponSlots?[...current,value]:current);} function toggleGear(id:string){setGear((current)=>current.includes(id)?current.filter((x)=>x!==id):[...current,id]);}

  function canEnter(target:StageId){const i=stageIndex(target);if(i===0)return true;if(i>=1&&!name.trim())return false;if(i>=2&&!scoresGenerated)return false;if(i>=3&&!raceId)return false;if(i>=4&&(!classIds.length||!classesQualified||!raceAllowsPath))return false;if(i>=5&&(!alignment||!alignmentAllowed))return false;if(i>=6&&!classFeaturesComplete)return false;if(i>=7&&!trainingComplete)return false;if(i>=8&&!vitalsComplete)return false;if(i>=9&&!equipmentComplete)return false;return true;}
  function stageComplete(id:StageId){if(id==="name")return Boolean(name.trim());if(id==="abilities")return scoresGenerated;if(id==="ancestry")return Boolean(raceId);if(id==="class")return classIds.length>0&&classesQualified&&raceAllowsPath;if(id==="alignment")return Boolean(alignment)&&alignmentAllowed;if(id==="features")return classFeaturesComplete;if(id==="training")return trainingComplete;if(id==="vitals")return vitalsComplete;if(id==="equipment")return equipmentComplete;return sealed;}
  function goNext(){const next=STAGES[stageIndex(stage)+1];if(next&&canEnter(next.id))setStage(next.id);} function goBack(){const i=stageIndex(stage);if(i>0)setStage(STAGES[i-1].id);} const nextStage=STAGES[stageIndex(stage)+1];const currentComplete=stageComplete(stage);

  function thiefValue(id:ThiefSkill){const base=THIEF_SKILLS.find((s)=>s.id===id)?.base??0;return Math.max(0,base+(raceId?THIEF_RACE[raceId]?.[id]??0:0)+dexThiefAdjustment(finalScores.dex,id)+thiefPoints[id]);}

  function renderFeatures(){
    const panels=[] as React.ReactNode[];
    if(singleFighter) panels.push(<section className="feature-card" key="fighter"><div className="feature-title"><Image src={CLASS_ART.fighter} alt="" width={76} height={76}/><div><h3>Fighter Specialization</h3><p>A single-class fighter may reserve extra weapon proficiency slots to specialize.</p></div></div><div className="feature-choice-row"><button type="button" className={fighterDecision==="none"?"selected":""} onClick={()=>chooseSpecialization(null)}>Remain broadly trained</button>{allowedWeapons.map((w)=><button type="button" key={w.id} className={fighterSpecialization===w.id?"selected":""} onClick={()=>chooseSpecialization(w.id)}>{w.name}<small>{w.id==="shortbow"||w.id==="longbow"?"3 slots total":"2 slots total"}</small></button>)}</div></section>);
    if(hasThief) panels.push(<section className="feature-card" key="thief"><div className="feature-title"><Image src={CLASS_ART.thief} alt="" width={76} height={76}/><div><h3>Thief Skill Training</h3><p>Allocate exactly 60 discretionary points. No more than 30 may go into one skill.</p></div><strong>{thiefAllocated}/60</strong></div><div className="thief-grid">{THIEF_SKILLS.map((skill)=><div key={skill.id}><span>{skill.label}</span><strong>{thiefValue(skill.id)}%</strong><small>+{thiefPoints[skill.id]} training</small><div><button type="button" onClick={()=>adjustThief(skill.id,-5)} disabled={thiefPoints[skill.id]===0}>−5</button><button type="button" onClick={()=>adjustThief(skill.id,5)} disabled={thiefPoints[skill.id]>=30||thiefAllocated>=60}>+5</button></div></div>)}</div></section>);
    if(hasWizard) panels.push(<section className="feature-card" key="wizard"><div className="feature-title"><Image src={CLASS_ART[classIds.includes("illusionist")?"illusionist":"mage"]} alt="" width={76} height={76}/><div><h3>Starting Spellbook</h3><p>First roll 3d4 for the size of the book. Read Magic and Detect Magic are required; you choose the remaining spells.</p></div></div>{wizardBookSize===null?<DiceTray roll={trayRoll?.title==="Starting Spellbook Size"?trayRoll:null} rolling={rolling} onRoll={rollWizardBook} actionLabel="Roll 3d4 Spellbook"/>:<><div className="spellbook-summary"><span>Book size</span><strong>{wizardKnownCount}/{wizardBookSize}</strong><small>Read Magic + Detect Magic already inscribed</small></div>{classIds.includes("illusionist")?<div className="specialty-choice"><span>Choose one Illusion specialty spell</span>{ILLUSION_SPECIALTY.map((spell)=><button type="button" key={spell} className={illusionSpell===spell?"selected":""} onClick={()=>{setIllusionSpell(spell);setWizardSpells((current)=>current.includes(spell)?current:[...current,spell]);}}>{spell}</button>)}</div>:null}<div className="spell-grid">{WIZARD_SPELLS.filter((spell)=>spell!==illusionSpell).map((spell)=><button type="button" key={spell} className={wizardSpells.includes(spell)?"selected":""} disabled={!wizardSpells.includes(spell)&&wizardKnownCount>=wizardTarget} onClick={()=>toggleWizardSpell(spell)}>{spell}</button>)}</div></>}</section>);
    if(hasPriest){const list=classIds.includes("druid")?DRUID_SPELLS:PRIEST_SPELLS;panels.push(<section className="feature-card" key="priest"><div className="feature-title"><Image src={CLASS_ART[classIds.includes("druid")?"druid":"cleric"]} alt="" width={76} height={76}/><div><h3>Memorized Priest Spells</h3><p>Choose your level-one prayers yourself. Wisdom gives {priestSlots-1} bonus first-level slot{priestSlots-1===1?"":"s"}.</p></div><strong>{priestSpells.length}/{priestSlots}</strong></div><div className="spell-grid">{list.map((spell)=><button type="button" key={spell} className={priestSpells.includes(spell)?"selected":""} onClick={()=>togglePriestSpell(spell)}>{spell}</button>)}</div></section>);}
    if(!panels.length) panels.push(<section className="feature-card confirmation-card" key="confirm"><Image src={CLASS_ART[classIds[0]]} alt="" width={94} height={94}/><h3>{classPathLabel(classIds)} Starting Features</h3><p>This path has no additional level-one allocation in the current creation profile. Confirm that you have reviewed the class features before continuing.</p><button type="button" className={`rpg-button ${featuresConfirmed?"selected":""}`} onClick={()=>setFeaturesConfirmed(true)}>{featuresConfirmed?"Features Confirmed":"Confirm Class Features"}</button></section>);
    return panels;
  }

  function renderStage(){
    if(stage==="name")return <div className="stage-content name-stage"><div className="stage-copy"><p className="stage-kicker">BEGIN YOUR LEGEND</p><h2>Name Your Adventurer</h2><p>Your character begins with a name. Nothing is generated for you.</p></div><div className="name-altar"><Image src="/assets/forge/icons/icon-record.png" alt="" width={118} height={118}/><label htmlFor="character-name">Character name</label><input id="character-name" value={name} onChange={(e)=>setName(e.target.value.slice(0,32))} placeholder="Enter a name..."/><small>{name.trim()?`${name.trim()} will be written into the chronicle.`:"The chronicle awaits its first name."}</small></div></div>;
    if(stage==="abilities")return <div className="stage-content abilities-stage"><div className="stage-copy"><p className="stage-kicker">LET THE DICE SPEAK</p><h2>Generate Ability Scores</h2><p>Choose your method, then throw each ability roll yourself.</p></div><div className="method-card-grid"><button type="button" className={rollMethod==="classic"?"selected":""} onClick={()=>chooseRollMethod("classic")}><Image src="/assets/forge/icons/icon-dice.png" alt="" width={76} height={76}/><strong>3d6 in order</strong><span>Three dice for each ability</span></button><button type="button" className={rollMethod==="heroic"?"selected":""} onClick={()=>chooseRollMethod("heroic")}><Image src="/assets/forge/icons/icon-random.png" alt="" width={76} height={76}/><strong>4d6 drop lowest</strong><span>Lowest die is discarded</span></button><button type="button" className={rollMethod==="manual"?"selected":""} onClick={()=>chooseRollMethod("manual")}><Image src="/assets/forge/icons/icon-record.png" alt="" width={76} height={76}/><strong>Assign manually</strong><span>Direct control</span></button></div>{rollMethod&&rollMethod!=="manual"?<div className="ability-roll-workbench"><div className="ability-dice-row">{ABILITIES.map((ability)=><div key={ability.id} className={`${abilityRolls[ability.id]?"rolled":""} ${currentAbility?.id===ability.id?"current":""}`}><span>{ability.short}</span><strong>{abilityRolls[ability.id]?baseScores[ability.id]:"—"}</strong><small>{ability.name}</small></div>)}</div><DiceTray roll={trayRoll} rolling={rolling} onRoll={currentAbility?rollNextAbility:undefined} actionLabel={currentAbility?`Roll ${currentAbility.name}`:undefined}/></div>:null}{rollMethod==="manual"?<div className="manual-score-grid">{ABILITIES.map((ability)=><label key={ability.id}><span>{ability.short}</span><input inputMode="numeric" value={manualScores[ability.id]} onChange={(e)=>changeManualScore(ability.id,e.target.value)} placeholder="3–18"/></label>)}</div>:null}</div>;
    if(stage==="ancestry")return <div className="stage-content ancestry-stage"><div className="stage-copy"><p className="stage-kicker">CHOOSE YOUR HERITAGE</p><h2>Ancestry</h2><p>Your ancestry modifies the rolled scores and opens its permitted class paths.</p></div><div className="art-card-grid race-art-grid">{RACES.map((item)=><button key={item.id} type="button" className={`art-choice ${raceId===item.id?"selected":""}`} onClick={()=>chooseRace(item.id)}><div className="art-choice-image"><Image src={RACE_ART[item.id]} alt="" fill sizes="180px"/></div><strong>{item.name}</strong><span>{item.summary}</span><small>{Object.keys(item.adjustments).length?Object.entries(item.adjustments).map(([key,value])=>`${key.toUpperCase()} ${signed(value)}`).join(" · "):"No ability adjustment"}</small></button>)}</div></div>;
    if(stage==="class"){if(!race)return null;const paths=[...race.availableClasses.map((id)=>[id] as ClassId[]),...race.multiclass];return <div className="stage-content class-stage"><div className="stage-copy"><p className="stage-kicker">CHOOSE YOUR CALLING</p><h2>Class Path</h2><p>Unavailable paths remain visible so your dice results matter.</p></div><div className="class-path-grid">{paths.map((ids)=>{const qualified=ids.every((id)=>qualifiesForClass(finalScores,id));const selected=classIds.length===ids.length&&ids.every((id,i)=>classIds[i]===id);return <button key={ids.join("-")} type="button" disabled={!qualified} className={`class-path-card ${selected?"selected":""}`} onClick={()=>choosePath(ids)}><div className="class-crests">{ids.map((id)=><div key={id}><Image src={CLASS_ART[id]} alt="" fill sizes="120px"/></div>)}</div><strong>{classPathLabel(ids)}</strong><span>{ids.length>1?"Multi-class path":classById(ids[0]).description}</span><small>{qualified?"AVAILABLE":"ABILITY REQUIREMENTS NOT MET"}</small></button>})}</div></div>}
    if(stage==="alignment")return <div className="stage-content alignment-stage"><div className="stage-copy"><p className="stage-kicker">SHAPE YOUR NATURE</p><h2>Alignment</h2><p>Your class path determines which choices remain legal.</p></div><div className="alignment-board"><Image src="/assets/forge/icons/icon-alignment.png" alt="" width={110} height={110}/><div className="alignment-grid-game">{ALIGNMENTS.map((item)=>{const permitted=classes.every((c)=>!c.allowedAlignments||c.allowedAlignments.includes(item));return <button key={item} type="button" disabled={!permitted} className={alignment===item?"selected":""} onClick={()=>setAlignment(item)}>{item}</button>})}</div></div></div>;
    if(stage==="features")return <div className="stage-content features-stage"><div className="stage-copy"><p className="stage-kicker">CLASS TRAINING</p><h2>Shape Your Starting Talents</h2><p>This stage changes to match your chosen class path. Every allocation and roll belongs to you.</p></div><div className="feature-stack">{renderFeatures()}</div></div>;
    if(stage==="training")return <div className="stage-content training-stage"><div className="stage-copy"><p className="stage-kicker">LEARN YOUR CRAFT</p><h2>Proficiencies</h2><p>Spend every available slot. Specialization reserves extra fighter slots where applicable.</p></div><div className="training-game-grid"><section><div className="training-heading"><Image src="/assets/forge/icons/icon-class.png" alt="" width={58} height={58}/><div><h3>Weapon Proficiencies</h3><span>{usedWeaponSlots}/{weaponSlots} slots</span></div></div><div className="choice-chip-grid">{allowedWeapons.map((weapon)=><button type="button" key={weapon.id} className={weaponProfs.includes(weapon.id)?"selected":""} onClick={()=>toggleWeapon(weapon.id)}><strong>{weapon.name}</strong><small>{weapon.id===fighterSpecialization?`Specialized · +${specializationExtra} slot${specializationExtra===1?"":"s"}`:weapon.damage}</small></button>)}</div></section><section><div className="training-heading"><Image src="/assets/forge/icons/icon-training.png" alt="" width={58} height={58}/><div><h3>Non-Weapon Proficiencies</h3><span>{nonWeaponProfs.length}/{nonWeaponSlots} slots</span></div></div><div className="choice-chip-grid">{NON_WEAPON_PROFICIENCIES.map((p)=><button type="button" key={p} className={nonWeaponProfs.includes(p)?"selected":""} onClick={()=>toggleNwp(p)}>{p}</button>)}</div></section></div></div>;
    if(stage==="vitals")return <div className="stage-content vitals-stage"><div className="stage-copy"><p className="stage-kicker">FATE'S LAST ROLLS</p><h2>Vitals & Starting Purse</h2><p>Choose each roll yourself and watch the forged dice land.</p></div><div className="vital-roll-grid"><div><Image src={STAT_ART.hp} alt="" width={120} height={120}/><h3>Starting Hit Points</h3><strong>{hp??"—"}</strong><button type="button" className="rpg-button" onClick={rollHp}>Roll HP Dice</button></div><div><Image src={STAT_ART.gold} alt="" width={120} height={120}/><h3>Starting Gold</h3><strong>{startingGold===null?"—":`${startingGold} gp`}</strong><button type="button" className="rpg-button" onClick={rollGold}>Roll Gold Dice</button></div>{canExceptional?<div><Image src="/assets/forge/icons/icon-dice.png" alt="" width={120} height={120}/><h3>Exceptional Strength</h3><strong>{exceptionalStrength===null?"18/—":`18/${String(exceptionalStrength).padStart(2,"0")}`}</strong><button type="button" className="rpg-button" onClick={rollExceptionalStrength}>Roll d100</button></div>:null}</div><DiceTray roll={trayRoll} rolling={rolling}/></div>;
    if(stage==="equipment")return <div className="stage-content equipment-stage"><div className="stage-copy"><p className="stage-kicker">PREPARE FOR THE ROAD</p><h2>Equipment</h2><p>Nothing is purchased unless you choose it.</p></div><div className="purse-banner"><Image src={STAT_ART.gold} alt="" width={62} height={62}/><div><span>Remaining purse</span><strong>{goldRemaining===null?"—":`${goldRemaining.toFixed(1)} gp`}</strong></div><small>{spent.toFixed(1)} gp selected</small></div><div className="equipment-sections"><section><h3>Armour</h3><div className="equipment-choice-grid">{allowedArmor.map((item)=><button key={item.id} type="button" className={armorId===item.id?"selected":""} onClick={()=>setArmorId(item.id)}><strong>{item.name}</strong><span>AC {item.ac}</span><small>{item.cost} gp</small></button>)}</div>{shieldAllowed?<button type="button" className={`shield-choice ${shield?"selected":""}`} onClick={()=>setShield((v)=>!v)}><Image src="/assets/forge/icons/badge-ac.png" alt="" width={54} height={54}/><span>{shield?"Shield equipped":"Add shield"}</span><small>{SHIELD_COST} gp</small></button>:null}</section><section><h3>General Gear</h3><div className="equipment-choice-grid gear">{GENERAL_GEAR.map((item)=><button key={item.id} type="button" className={gear.includes(item.id)?"selected":""} onClick={()=>toggleGear(item.id)}><strong>{item.name}</strong><small>{item.cost} gp</small></button>)}</div></section></div></div>;
    return <div className="stage-content record-stage"><div className="stage-copy"><p className="stage-kicker">THE CHRONICLE AWAITS</p><h2>Final Character Record</h2><p>Review the complete first-level record before sealing it.</p></div><div className="final-hero"><div className="final-art">{raceId?<Image src={RACE_ART[raceId]} alt="" fill sizes="260px"/>:null}</div><div><span>{race?.name}</span><h3>{name}</h3><p>{classPathLabel(classIds)} · {alignment}</p><div className="final-score-line">{ABILITIES.map((a)=><span key={a.id}><small>{a.short}</small><strong>{finalScores[a.id]}{a.id==="str"&&exceptionalStrength!==null?`/${String(exceptionalStrength).padStart(2,"0")}`:""}</strong></span>)}</div></div></div><button type="button" className={`seal-button ${sealed?"sealed":""}`} onClick={()=>setSealed(true)}>{sealed?"Character Sealed":"Seal Character Record"}</button></div>;
  }

  return <main className="game-forge-shell"><header className="game-header"><div className="brand-mark"><Image src="/assets/forge/icons/icon-star.png" alt="" width={58} height={58}/><div><h1>Character Forge</h1><p>Forge your legend. The realm awaits.</p></div></div></header><div className="game-workspace"><nav className="stage-rail" aria-label="Character creation stages">{STAGES.map((item)=>{const active=stage===item.id,complete=stageComplete(item.id),unlocked=canEnter(item.id);return <button key={item.id} type="button" disabled={!unlocked} className={`${active?"active":""} ${complete?"complete":""}`} onClick={()=>unlocked&&setStage(item.id)}><span className="rail-icon"><Image src={item.icon} alt="" width={42} height={42}/></span><span className="rail-text"><strong>{item.number}. {item.label}</strong><small>{item.hint}</small></span>{complete?<span className="rail-check">✓</span>:null}</button>})}<div className="rail-event"><span>Latest</span><p>{eventLog[0]??"The forge is waiting for your first choice."}</p></div></nav><section className="main-stage-panel">{renderStage()}<div className="stage-navigation"><button type="button" className="nav-back" disabled={stageIndex(stage)===0} onClick={goBack}>‹ Back</button><div className="stage-dots">{STAGES.map((item)=><span key={item.id} className={stage===item.id?"active":stageComplete(item.id)?"complete":""}/>)}</div>{nextStage?<button type="button" className="nav-next" disabled={!currentComplete||!canEnter(nextStage.id)} onClick={goNext}>Continue to {nextStage.label} ›</button>:<span className="nav-finished">{sealed?"Ready for Adventure":"Seal the record to finish"}</span>}</div></section><aside className="character-record"><div className="record-crest"><Image src={raceId?RACE_ART[raceId]:"/assets/forge/icons/icon-record.png"} alt="" width={92} height={92}/></div><p className="record-kicker">CHARACTER RECORD</p><h2>{name.trim()||"Unnamed Adventurer"}</h2><p className="record-subtitle">{race?.name??"Ancestry unchosen"} · {classPathLabel(classIds)}</p><p className="record-alignment">{alignment||"Alignment unchosen"}</p><div className="record-badges"><div><Image src={STAT_ART.hp} alt="" width={60} height={60}/><span>HP</span><strong>{hp??"—"}</strong></div><div><Image src={STAT_ART.ac} alt="" width={60} height={60}/><span>AC</span><strong>{ac??"—"}</strong></div><div><Image src={STAT_ART.thac0} alt="" width={60} height={60}/><span>THAC0</span><strong>{thac0??"—"}</strong></div><div><Image src={STAT_ART.gold} alt="" width={60} height={60}/><span>GOLD</span><strong>{goldRemaining===null?"—":goldRemaining.toFixed(1)}</strong></div></div><div className="record-abilities">{ABILITIES.map((ability)=><div key={ability.id}><span className="ability-token">{ability.short}</span><span>{ability.name}</span><strong>{scoresGenerated?finalScores[ability.id]:"—"}{ability.id==="str"&&exceptionalStrength!==null?`/${String(exceptionalStrength).padStart(2,"0")}`:""}</strong></div>)}</div>{scoresGenerated?<><div className="record-section-title">Derived</div><div className="derived-grid"><div><span>Melee Hit</span><strong>{strEffects?signed(strEffects.hit):"—"}</strong></div><div><span>Melee Damage</span><strong>{strEffects?signed(strEffects.damage):"—"}</strong></div><div><span>Weight</span><strong>{strEffects?`${strEffects.weight} lb`:"—"}</strong></div><div><span>Missile</span><strong>{dexEffects?signed(dexEffects.missile):"—"}</strong></div><div><span>System Shock</span><strong>{conEffects?`${conEffects.systemShock}%`:"—"}</strong></div><div><span>Languages</span><strong>{intEffects?.languages??"—"}</strong></div><div><span>Magic Defence</span><strong>{wisEffects?signed(wisEffects.magicDefense):"—"}</strong></div><div><span>Reaction</span><strong>{chaEffects?signed(chaEffects.reaction):"—"}</strong></div></div></>:null}{savingThrows?<><div className="record-section-title">Saving Throws</div><div className="save-grid"><div><span>Poison / Death</span><strong>{savingThrows.paralyzationPoisonDeath}</strong></div><div><span>Rod / Staff / Wand</span><strong>{savingThrows.rodStaffWand}</strong></div><div><span>Petrify / Polymorph</span><strong>{savingThrows.petrificationPolymorph}</strong></div><div><span>Breath</span><strong>{savingThrows.breathWeapon}</strong></div><div><span>Spell</span><strong>{savingThrows.spell}</strong></div></div></>:null}<div className="record-details"><div><span>Armour</span><strong>{armor?.name??"Unchosen"}{shield?" + shield":""}</strong></div><div><span>Weapon Training</span><strong>{classes.length?`${usedWeaponSlots}/${weaponSlots}`:"—"}</strong></div><div><span>Other Training</span><strong>{classes.length?`${nonWeaponProfs.length}/${nonWeaponSlots}`:"—"}</strong></div>{fighterSpecialization?<div><span>Specialization</span><strong>{WEAPONS.find((w)=>w.id===fighterSpecialization)?.name}</strong></div>:null}{hasThief&&thiefFeatureComplete?<div><span>Thief Training</span><strong>60 pts allocated</strong></div>:null}{hasWizard&&wizardBookSize?<div><span>Spellbook</span><strong>{wizardBookSize} spells</strong></div>:null}{hasPriest?<div><span>Priest Slots</span><strong>{priestSlots} × 1st</strong></div>:null}</div><div className="record-status"><Image src={sealed?"/assets/forge/icons/icon-star.png":"/assets/forge/icons/icon-info.png"} alt="" width={34} height={34}/><p>{sealed?"This character is sealed and ready for the adventure engine.":`Stage ${stageIndex(stage)+1} of ${STAGES.length}: ${STAGES[stageIndex(stage)].label}`}</p></div></aside></div></main>;
}
