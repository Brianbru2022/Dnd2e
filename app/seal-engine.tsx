"use client";
import {useEffect,useMemo,useState} from "react";
import {ABILITIES,CLASSES,RACES,type AbilityKey,type AbilityScores,type ClassId,type RaceId} from "./rules";
import type {CharacterFinalDetails,CharacterRecord,CharacterSex,MagicRecord,PortraitForgeRecord,ClassProgressionSnapshot,ProficiencyRecord} from "./character-record";
import {validateCharacterRecord} from "./character-record";
import {deriveAllAbilities,raceTraits} from "./core-traits";
import {hpProgression,proficiencyGains,savesFor,spellSlotsFor,thac0For,thiefBackstabMultiplier,warriorAttacksPerRound,xpFor} from "./class-progression";
import {NONPROFICIENCY_PENALTY,SPECIALIZATION_RULES} from "./proficiency-rules";
import {armourById,gearById,weaponById} from "./equipment-catalog";
import {systemicForGear} from "./equipment-systemics";
import {MAGIC_RULES_PROFILE,spellByName} from "./magic-catalog";
function num(t:string|undefined|null){const n=Number.parseFloat((t??"").replace(/[^0-9.-]/g,""));return Number.isFinite(n)?n:0}function text(s:string){return document.querySelector(s)?.textContent?.trim()??""}function readRace():RaceId|null{const t=text(".record-subtitle");return RACES.find(r=>t.includes(r.name))?.id??null}function readClasses():ClassId[]{const t=text(".record-subtitle");return CLASSES.filter(c=>t.includes(c.name)).map(c=>c.id)}function readScores():AbilityScores{const out={} as AbilityScores;document.querySelectorAll(".record-abilities > div").forEach(row=>{const k=row.querySelector(".ability-token")?.textContent?.trim().toLowerCase() as AbilityKey|undefined;const v=Number.parseInt(row.querySelector("strong")?.textContent??"",10);if(k&&ABILITIES.some(a=>a.id===k)&&Number.isFinite(v))out[k]=v});return out}function readSave(label:string){const rows=Array.from(document.querySelectorAll(".save-grid > div"));const row=rows.find(r=>r.querySelector("span")?.textContent?.includes(label));return Number.parseInt(row?.querySelector("strong")?.textContent??"0",10)||0}function readSelectedButtons(selector:string){return Array.from(document.querySelectorAll(selector)).map(b=>b.querySelector("strong")?.textContent?.trim()??b.textContent?.trim()??"").filter(Boolean)}function safeJson<T>(key:string,fallback:T):T{try{return JSON.parse(localStorage.getItem(key)??"") as T}catch{return fallback}}function exceptionalFromDom(){const m=text(".record-subtitle").match(/18\/(\d{1,2}|00)/);if(!m)return null;return m[1]==="00"?100:Number.parseInt(m[1],10)}
function progressionSnapshot(classId:ClassId):ClassProgressionSnapshot{const c=CLASSES.find(item=>item.id===classId)!;const level=1;const gains=proficiencyGains(c.group,level);return{classId,level,xp:0,nextLevelXp:xpFor(classId,2),thac0:thac0For(c.group,level),savingThrows:savesFor(c.group,level),hpProgression:hpProgression(classId,level),spellSlots:spellSlotsFor(classId,level),attacksPerRound:c.group==="Warrior"?warriorAttacksPerRound(level):undefined,backstabMultiplier:classId==="thief"?thiefBackstabMultiplier(level):undefined,weaponBonusSlots:gains.weaponBonusSlots,nonweaponBonusSlots:gains.nonweaponBonusSlots}}
function idsFromNames(names:string[],kind:"weapon"|"gear"){return names.map(n=>n.toLowerCase()).map(n=>{if(kind==="weapon"){const w=["longsword","shortsword","battleaxe","warhammer","mace","quarterstaff","dagger","spear","shortbow","longbow","sling"].find(id=>n.includes(id.replace("sword"," sword").replace("bow"," bow")));return w??n.replace(/[^a-z0-9]+/g,"-")}const map:Record<string,string>={backpack:"backpack",bedroll:"bedroll",rope:"rope-hemp",lantern:"lantern-hooded",oil:"oil",rations:"rations",waterskin:"waterskin"};return Object.entries(map).find(([k])=>n.includes(k))?.[1]??n.replace(/[^a-z0-9]+/g,"-")})}
function spellRecords(names:string[]){return names.map(n=>spellByName(n)).filter(Boolean) as NonNullable<ReturnType<typeof spellByName>>[]}
function buildRecord():Partial<CharacterRecord>{const race=readRace(),classes=readClasses(),scores=readScores();const magic=safeJson<MagicRecord>("forge-magic-ledger",{learned:[],memorizedWizard:[],memorizedPriest:[],attempts:[]});const finalDetails=safeJson<CharacterFinalDetails>("forge-final-details",{languages:[],age:null,heightIn:null,weightLb:null,portrait:null});const portraitForge=safeJson<PortraitForgeRecord|undefined>("forge-portrait-forge",undefined);const sex=localStorage.getItem("character-forge-sex") as CharacterSex|null;const bard=safeJson<Record<string,number>>("forge-bard-skills",{});const ranger=localStorage.getItem("forge-ranger-species-enemy")??undefined;const profs=safeJson<ProficiencyRecord[]>("forge-proficiencies",[]);const exceptionalStrength=exceptionalFromDom();const warrior=classes.some(id=>["fighter","ranger","paladin"].includes(id));const weaponNames=readSelectedButtons(".training-game-grid section:first-child button.selected");const gearNames=readSelectedButtons(".equipment-choice-grid.gear button.selected");const weaponIds=idsFromNames(weaponNames,"weapon");const gearIds=idsFromNames(gearNames,"gear");const armourName=text(".record-details > div:first-child strong");const armour=armourById(armourName.toLowerCase().includes("chain")?"chain":armourName.toLowerCase().includes("plate")?"plate":armourName.toLowerCase().includes("scale")?"scale":armourName.toLowerCase().includes("studded")?"studded":armourName.toLowerCase().includes("leather")?"leather":"none")??null;const richGear=gearIds.map(id=>gearById(id)).filter(Boolean).map(g=>({...g!,systemic:systemicForGear(g!)}));const known=[...new Set([...magic.learned,...magic.memorizedPriest])];const memorized=[...magic.memorizedWizard,...magic.memorizedPriest];return{schemaVersion:1,sealedAt:new Date().toISOString(),identity:{name:text(".character-record h2"),sex},race:race??undefined,classes,classProgression:classes.map(progressionSnapshot),alignment:text(".record-alignment"),abilities:scores,derivedAbilities:deriveAllAbilities(scores,exceptionalStrength,warrior),racialTraits:race?raceTraits(race):undefined,hp:num(text(".record-badges > div:nth-child(1) strong")),ac:num(text(".record-badges > div:nth-child(2) strong")),thac0:num(text(".record-badges > div:nth-child(3) strong")),savingThrows:{paralyzationPoisonDeath:readSave("Poison"),rodStaffWand:readSave("Rod"),petrificationPolymorph:readSave("Petrify"),breathWeapon:readSave("Breath"),spell:readSave("Spell")},gold:num(text(".record-badges > div:nth-child(4) strong")),proficiencies:profs,proficiencyRules:{nonproficiencyPenalties:{...NONPROFICIENCY_PENALTY},specialization:SPECIALIZATION_RULES,catalogueVersion:"core-pseudo-2e-v1"},classChoices:{rangerSpeciesEnemy:ranger,bardSkills:bard},magic,magicRules:{catalogueVersion:MAGIC_RULES_PROFILE.catalogueVersion,knownSpellRecords:spellRecords(known),memorizedSpellRecords:spellRecords(memorized),rules:MAGIC_RULES_PROFILE},finalDetails,portraitForge,equipment:{armour:armourName||null,shield:armourName.includes("shield"),weapons:weaponNames,gear:gearNames},equipmentRules:{catalogueVersion:"core-pseudo-2e-equipment-v2-systemic",coinageVersion:"five-coin-standard-v1",weapons:weaponIds.map(id=>weaponById(id)).filter(Boolean) as NonNullable<ReturnType<typeof weaponById>>[],armour,gear:richGear},encumbrance:{carriedLb:num(text(".encumbrance-grid > div:nth-child(1) strong")),allowanceLb:num(text(".encumbrance-grid > div:nth-child(2) strong")),category:text(".encumbrance-title strong"),movement:num(text(".encumbrance-grid > div:nth-child(4) strong"))},rollHistory:[...magic.attempts,{appearance:portraitForge?.appearance??{},portraitPrompt:portraitForge?.generatedPrompt??""}]}}
export default function SealEngine(){const[version,setVersion]=useState(0),[errors,setErrors]=useState<string[]>([]),[sealed,setSealed]=useState(false);useEffect(()=>{const obs=new MutationObserver(()=>setVersion(v=>v+1));obs.observe(document.body,{subtree:true,childList:true,characterData:true,attributes:true});const handler=()=>setVersion(v=>v+1);window.addEventListener("forge-magic-updated",handler);window.addEventListener("forge-portrait-requested",handler);return()=>{obs.disconnect();window.removeEventListener("forge-magic-updated",handler);window.removeEventListener("forge-portrait-requested",handler)}},[]);const button=useMemo(()=>typeof document!=="undefined"?document.querySelector(".seal-button") as HTMLButtonElement|null:null,[version]);useEffect(()=>{if(!button)return;const onClick=(e:Event)=>{e.preventDefault();e.stopImmediatePropagation();const candidate=buildRecord();const result=validateCharacterRecord(candidate);setErrors(result.errors);if(!result.valid){setSealed(false);button.classList.remove("sealed");button.textContent="Resolve Character Errors";return}const finalRecord={...candidate,sealedAt:new Date().toISOString()} as CharacterRecord;localStorage.setItem("forge-character-record",JSON.stringify(finalRecord));setSealed(true);button.classList.add("sealed");button.textContent="Character Sealed";window.dispatchEvent(new CustomEvent("forge-character-sealed",{detail:finalRecord}))};button.addEventListener("click",onClick,true);return()=>button.removeEventListener("click",onClick,true)},[button]);if(!errors.length&&!sealed)return null;return <aside className={`seal-validation ${sealed?"success":"failure"}`}><strong>{sealed?"Character record sealed":"Character cannot be sealed yet"}</strong>{sealed?<p>The canonical CharacterRecord has been saved and is ready for the adventure engine.</p>:<ul>{errors.map(e=><li key={e}>{e}</li>)}</ul>}</aside>}
